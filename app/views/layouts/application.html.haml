!!!
%head
  = stylesheet_link_tag    "application", media: "all"
  = javascript_include_tag "application"
  = csrf_meta_tags
  %title
    TownNotNice

%div.main
  %h1 TownNotNice
  %div.username_div
    %li
      - if current_user.present?
        = current_user.username
        - if current_user.admin?
          = '(Admin)'
          = link_to 'Embed Video', new_video_path, class: 'embed_video'
        = link_to 'Log Out', log_out_path, method: :delete, class: 'log_in'

      - else
        Your UserName
        %li= link_to 'Sign Up', new_user_path

%body
  = yield
